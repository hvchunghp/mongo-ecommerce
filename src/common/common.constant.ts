export const regexEmail = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/g;

export enum EReturnStatus {
    SUCCESS = 'success',
    ERROR = 'error',
}

export enum ESortBy {
    ASC = 'asc',
    DESC = 'desc',
}
