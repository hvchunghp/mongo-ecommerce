export const regexEmail = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/g;

export enum EReturnStatus {
    SUCCESS = 'success',
    ERROR = 'error',
}

export enum ESortBy {
    ASC = 'asc',
    DESC = 'desc',
}

export const CommonUnSelect = {
    __v: 0,
    updatedAt: 0,
    createdAt: 0,
};
